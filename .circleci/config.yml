version: 2
jobs:
  build:
    docker:
      - image: circleci/golang:1.9      
    working_directory: /go/src/github.com/Brightspace/terraform-provider-shell
    steps:
      - checkout

      - run: go get -v -t -d ./...
      - run: go test -v ./...
  release:
    docker:
      - image: goreleaser/goreleaser:v0.82
    working_directory: /go/src/github.com/Brightspace/terraform-provider-shell